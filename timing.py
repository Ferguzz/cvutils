import timeit

"""Just uncomment the section corresponding to the function you wish to time.  The number of runs and repeats can be easily altered.  show() and wait() are not here because that would just be silly."""

# t = timeit.Timer('cvutils.blackandwhite(im)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'blackandwhite() takes aprox %f msec' % (1000*(min(t.repeat(number = 10000, repeat = 3)))/10000)

# t = timeit.Timer('cvutils.brightness(im, 100)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'brightness() takes aprox %f msec' % (1000*(min(t.repeat(number = 1000, repeat = 3)))/1000)

# t = timeit.Timer('cvutils.clone(im)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'clone() takes aprox %f msec' % (1000*(min(t.repeat(number = 1000, repeat = 3)))/1000)

# t = timeit.Timer('cvutils.contrast(im, 2)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'contrast() takes aprox %f msec' % (1000*(min(t.repeat(number = 1000, repeat = 3)))/1000)

# t = timeit.Timer('cvutils.create(im, True)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'create() takes aprox %f msec' % (1000*(min(t.repeat(number = 1000, repeat = 3)))/1000)

# t = timeit.Timer('cvutils.gaussiannoise(im)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'gaussiannoise() takes aprox %f msec' % (1000*(min(t.repeat(number = 10, repeat = 3)))/10)

# t = timeit.Timer('cvutils.rotate(im, 20)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'rotate() takes aprox %f msec' % (1000*(min(t.repeat(number = 1000, repeat = 3)))/1000)

# t = timeit.Timer('cvutils.saltandpepper(im, 0.1)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'saltandpepper() takes aprox %f msec' % (1000*(min(t.repeat(number = 1000, repeat = 3)))/1000)

# t = timeit.Timer('cvutils.sample(im)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'sample() takes aprox %f msec' % (1000*(min(t.repeat(number = 1000, repeat = 3)))/1000)

# t = timeit.Timer('cvutils.zoom(im, 2)', "import cvutils, cv; im = cv.LoadImage('kitten.jpg')")
# print 'zoom() takes aprox %f msec' % (1000*(min(t.repeat(number = 1000, repeat = 3)))/1000)





